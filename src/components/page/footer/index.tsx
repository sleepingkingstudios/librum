import * as React from 'react';

const defaultClassName = 'mt-3 shrink-0 text-center text-muted sm:text-left';

export const PageFooter = (): JSX.Element => (
  <footer className={defaultClassName}>
    <hr className="hr-fancy mb-2" />

    What lies beyond the furthest reaches of the sky?
  </footer>
);
